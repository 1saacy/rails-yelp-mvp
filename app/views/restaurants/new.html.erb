<%= simple_form_for @restaurant do |f| %>
  <%= f.input :name%>
  <%= f.input :address%>
  <%= f.input :phone_number%>
  <%= f.input :category, collection: ["french", "italian", "chinese", "japanese", "belgian"]%>
  <%= f.submit "add a restaurant", class: "btn btn-primary" %>
<% end %>
